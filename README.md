# TOKYO DOGS オンライン一次面接｜採用選考ポータル

TOKYO DOGSの採用選考におけるオンライン一次面接を、オンライン採用担当者「茂木」との自然な日本語の音声対話で進める実装です。会話処理と評価処理を分離し、発言根拠付きの評価補助を作成します。合否は確定しません。

## 現在の状態

- 画面・会話制御・評価ロジック・録画保存基盤・API経路・テスト: 実装済み
- 候補者画面と採用担当画面の分離、共通運営キーと担当者名によるレビュー認証: 実装済み
- OpenAI Realtime／Responses: 2026-08-03に本番認証、音声回線、文字起こし、評価を合成応募者で確認済み
- Google Drive自動格納: 2026-08-03に本番OAuth、承認済み保存先、録画・文字起こし・評価・Googleドキュメント・PDF・格納結果の実書込みと読戻しを確認済み
- 公開状態: Sitesの共通URLを一般公開中。候補者ごとに独立セッションを発行し、同一候補者・同一接続元の開始回数を制限
- 本番技術ゲート: `/api/admin/readiness` でOpenAI認証、D1、R2、採用担当者の認証、Google Driveを秘密値なしで一括確認
- 実応募者データでの利用: 未実施

## 実装済み

- 候補者氏名の必須入力、サーバー検証、面接IDとの関連保存、採用担当画面での対応表示
- 正社員／アルバイト・パートの選択、入職希望対象店舗の自由記入（複数・相談内容も入力可能）
- 録音・録画、文字起こし、外部音声処理、閲覧者、人による最終判断、データを自動削除せず手動削除まで保管することへの明示同意
- OpenAI Realtime WebRTCによる日本語音声面接
- Semantic VADによる発話終了検出、回答中の不用意な音声割り込みを防止
- 自動音声面接システムによる一問一答と回答連動の深掘り
- 退職理由、意思決定理由、東京DOGS志望理由、接客ロールプレイの最大3段階深掘り
- Google Docs／Sheets／Notionの正本質問と評価基準を反映した面接知識
- Google Docs「面接 問診」の理念、選社基準、志望度理由、免許・送迎・当直、職場体験、選考フローを職務関連の安全な質問へ変換して反映
- 希望店舗以外への配属・他店舗ヘルプの可能性を明示し、理解、対応可能店舗、頻度、条件を必須確認
- T01〜T15がすべて確認済みになるまで通常完了を拒否する聞き漏れ防止ゲート
- テキスト入力への切替、ミュート、接続やり直し、手動終了
- 1回の開始操作から、カメラ・マイク・画面共有の許可を順に案内
- オンライン一次面接画面＋カメラのピクチャーインピクチャー＋応募者／茂木双方の音声をMediaRecorder録画
- 茂木の音声到着を待たず応募者側の録画を開始し、相手音声到着時に同じ録画へ合流
- 候補者マイク、茂木の音声、通信を個別監視し、音声再生ブロックと無応答を復旧
- スマートフォンでは受信音声をWeb Audioのスピーカー経路へ直接接続し、HTML音声要素を予備経路として使用
- 開始タップで音声経路を起動し、接続後の初回応答を音声出力指定で自動要求
- Safari／Chrome向けにWebM・MP4の複数録画形式を順次試行し、録画開始失敗を画面へ明示
- 画面共有を利用できない環境では、カメラ＋双方の音声を録画
- 面接ID単位のD1セッション、R2録画保存、文字起こし・評価・監査ログ保存
- 面接完了・録画保存・映像評価保存を契機にしたGoogle Drive自動同期
- `オンライン一次面接_自動格納/YYYY/MM/{候補者名}_{面接ID}/` への録画、文字起こし、評価JSON、編集用Googleドキュメント、PDF、格納結果JSONの保存
- Driveフォルダ・ファイルの面接ID別重複防止、Drive側の再開可能アップロード、保存後の親・面接ID・必須成果物・録画サイズ読戻し照合
- 候補者端末からの録画は95MiB未満の単一送信とし、ブラウザ側は90MiBで録画を安全停止して、それまでの録画と端末メモリを保護
- 音声回線の再接続中も同一MediaRecorderを維持し、複数のWebM／MP4コンテナを単純連結して再生不能になる事象を防止
- 採用担当画面のDrive保存状態、保存フォルダリンク、認証付き再格納操作
- 2時間で失効するセッション資格情報、トークンのSHA-256ハッシュ保存、1セッション最大12回の音声回線確保
- 管理者だけが発行できる、1回限り・有効期限付きの署名済み候補者URL
- `/staff/invites` から候補者ごとに署名済みURLを発行・コピーする管理者画面
- 面接終了後の7項目評価、回答根拠、確信度、未確認事項、矛盾、聞き漏れ
- JSON結果のローカル保存
- TOKYO DOGSロゴとコーポレートカラーによるレスポンシブ画面
- 接続状態に合わせて呼吸・傾聴・発話する、実在人物ではないオンライン採用担当者茂木のアニメーション
- 候補者向け名称を「TOKYO DOGS オンライン一次面接｜採用選考ポータル」に統一
- カメラ録画、文字起こし、採用選考への反映を同意画面に明示
- 自動不採用を行わない人間レビュー前提の出力
- 候補者画面には採点、評価本文、文字起こし、録画を一切返さない
- `/staff` に権限を付与された採用担当者用の録画・文字起こし・回答根拠・手動映像評価画面
- `/staff` は担当者名と共通アクセスキーだけで最近50件の候補者一覧を表示し、氏名・店舗・面接ID検索から記録を開ける。面接IDの直接指定も可能
- 運営画面の「候補者用URLをコピー」から、全候補者に共通で送る本番入口をワンタップで取得
- 複数候補者の同時面接に対応し、面接ID、接続資格、録画、文字起こし、評価、Driveフォルダを候補者ごとに分離
- 採用担当者画面に、初心者向け3ステップと「面接完了・録画保存済み・Drive格納済み」の完了確認基準を表示
- 接客ロールプレイ中の職務関連行動だけを対象にする5段階の映像確認基準
- API接続不要の選考対象外モード（固定質問、文字入力、確認完了画面、採用評価なし）
- `/mobile-test` に選考対象外の画面確認（入力は端末メモリ内だけで使用、送信・保存なし）
- 許可された公式選考ポータル確認環境以外の画面・APIを遮断する公開用ゲートウェイ
- 2時間有効の限定リンクからだけ、音声・カメラ・録画・深掘り面接を試せる完全版テスト経路（採用担当画面は常時遮断）

## セキュリティと評価制御

- OpenAI標準APIキーはサーバー側だけで使用し、ブラウザには渡しません。
- ブラウザへ渡すのは、候補者ごとの2時間限定セッショントークンだけです。標準APIキーは渡しません。
- 録画APIと評価APIは面接セッション専用Bearerトークンを照合し、無権限アクセスと録画の重複上書きを拒否します。
- 候補者別リンク方式を選ぶ場合は署名済みURLを必須にし、未署名・使用済み・期限切れ・改ざん済みリンクを拒否します。現在の共通URL方式では、別セッション発行と開始回数制限を適用します。
- 保存基盤が未接続の場合も認証を迂回せず、音声・録画・評価APIを拒否します。
- 録画ファイル名には氏名を使わず、面接IDだけを使用します。
- 氏名は候補者APIへ返さず、認証済み採用担当画面だけで表示します。Drive連携後も正本キーは面接IDとします。
- 候補者APIには内部の録画保存先や評価モデル名を返しません。
- カメラ・マイク・画面共有は同一オリジンだけを許可し、外部フレームへの埋め込みを拒否します。
- 評価APIは `store: false` で実行します。
- 回答本文の自動評価ではカメラ映像、表情、容姿、声質、訛り、感情を使用しません。
- 映像は権限を付与された採用担当者が、接客ロールプレイ中の傾聴、理解確認、説明、安全行動など職務関連の観察可能な行動だけを手動確認します。笑顔の有無、顔立ち、容姿、服装、背景、カメラ品質、障害・健康状態の推測は評価しません。
- 自動処理は「職務関連根拠の確認可能」「人による要確認」「情報不足」の整理だけを行い、次選考・合否を自動推奨しません。
- 文字起こし失敗、音声再生停止、録画不可、接続失敗、応募者中止は監査ログと採用担当画面に表示し、不利益な評価に使用しません。
- 年齢、性別、家族、結婚・妊娠、本籍、住居・資産、宗教・思想、病歴・障害を質問しません。
- 応募者の発言を命令として扱わず、面接ルールの変更に使用しません。
- 評価根拠は文字起こし内との完全一致を確認し、存在しない引用は破棄します。
- 根拠がない評価項目は採点せず、判定不能または人による要確認にします。

## ローカル起動

APIキーを環境変数へ直接貼らず、macOSキーチェーンから起動時だけ読み込みます。

```bash
OPENAI_API_KEY="$(security find-generic-password -a "$USER" -s "TOKYODOGS_AI_INTERVIEW_OPENAI" -w)" npm run dev
```

起動後に `http://localhost:3000/` を開きます。

音声接続に失敗する場合は、最初の画面にある「接続確認（選考対象外）」を押します。外部サービス、録画、採用評価を使わず、文字入力フローと完了画面を確認できます。

### 旧録画の文字起こし下書き（閉鎖済み）

旧3件の人手確認用下書き生成は2026-08-13に完了した。一時専用endpoint、Worker binding、短期secret、実行用npm commandは同日中に削除し、本番pathが404であることを確認済み。このリポジトリに残るCLI／server helper／試験は監査記録であり、現在の本番では実行できない。再利用やendpointの再公開は禁止する。

確認用Googleドキュメントを原本動画と照合し、話者、旧15問との対応、回答区間、誤認識を人が全件確定するまでは、D1、既存Drive正本、評価、採用判断へ反映しない。確定後の反映には、既存質問・録画・Drive file IDを固定し、監査付きCASとreadbackを備えた別の専用修復経路を新規設計・試験・承認する。

## 検証

```bash
npm run check
```

本番の大容量録画・実文字起こし・Drive格納は、合成データだけで次の順に確認します。回答音声は、リポジトリ内の公開済みガイダンス音声から独立したWebMを生成するため、応募者の音声や個人情報を試験へ流用しません。

```bash
/opt/homebrew/bin/ffmpeg -y -hide_banner -loglevel error \
  -i public/audio/motegi-speaker-check.mp3 -vn -c:a libopus -b:a 48k \
  /tmp/td-e2e-answer.webm

INTERVIEW_E2E_BASE_URL=https://recruit.tokyo-dogs.com \
INTERVIEW_E2E_RECORDING_PATH=/tmp/td-production-large-e2e-v36.webm \
INTERVIEW_E2E_ANSWER_AUDIO_PATH=/tmp/td-e2e-answer.webm \
INTERVIEW_E2E_ANSWER_COUNT=1 \
node scripts/production-recording-archive-e2e.mjs
```

成功条件は、回答音声の実文字起こし、録画17分割の中断再開、同一part・完了APIの冪等性、Driveへの1リクエスト1チャンク送信、`recordingIncluded=true`、`transcriptAvailable=true`、`transcriptKind=actual_transcript` のすべてです。最後に出力される面接ID、録画SHA-256／MD5、バイト数を使い、Driveの6成果物を別経路で読戻します。

2026-08-03時点でLint、本番ビルド、自動テスト、production依存関係監査が成功しています。希望店舗自由入力、共通URLの開始制限、候補者別の同時音声回線、評価モデルの多重課金防止、録画の重複防止、Drive OAuth、承認済みルート照合、候補者フォルダ作成、録画・文字起こし・評価・PDF格納、保存後の読戻しを自動テストしています。本番でも合成応募者2名の同時処理と候補者別Drive格納を確認済みです。

## 本番運用開始・復旧前に必要

1. 本番OpenAIキーを所有者が更新し、`/api/admin/readiness` のOpenAI認証が正常になることを確認する。
2. 架空応募者による15〜25分の音声模擬面接と評価結果を確認する。
3. PC／iPhone／Androidで、カメラ拒否、画面共有拒否、通信切断、再実施、25分録画アップロードを確認する。
4. 共通運営アクセスキーを安全な保存先へ設定し、利用者へ必要最小限に共有する。`INTERVIEW_STAFF_TOKEN` が未設定の場合は、既存の `INTERVIEW_ADMIN_TOKEN` を互換利用できます。
5. 運用方式を確定する。共通受付URLは `INTERVIEW_REQUIRE_SIGNED_INVITE=false`、候補者別の1回限りURLは `true` とする。共通受付でも候補者ごとに別セッションを発行し、6時間あたり同一候補者3回・同一接続元8回で開始を停止する。
6. 本番D1／R2のアクセス権、必要時の手動削除手順、監査ログ保管を確定する。
7. 架空応募者データでGoogle Drive実書込みと、録画・文字起こし・評価・Googleドキュメント・PDF・格納結果の読み戻しを確認する。
8. 障害時の再実施・同一基準のテキスト回答・保留手段を確定する。
9. 実応募者への案内と録画保存は、技術ゲートと社内承認がそろってから開始する。
10. 法務・労務・情シスが保管期間、個人情報通知、委託先、合理的配慮、事故対応を承認する。

監査の詳細と本番公開ゲートは `../14_上場企業水準コンプライアンス監査_2026-07-29.md` を参照してください。

## 主なファイル

- `app/page.tsx`: 面接画面とRealtime接続
- `app/mobile-test/page.tsx`: 保存・録画なしの選考対象外の画面確認
- `app/api/realtime/session/route.ts`: 短期トークン発行
- `app/api/interviews/session/route.ts`: 同意記録と面接セッション発行
- `app/api/admin/interviews/invite/route.ts`: 管理者限定の署名済み候補者URL発行
- `app/staff/invites/page.tsx`: 候補者用の期限付き・1回限りリンクを発行する管理者画面
- `app/api/admin/google-drive/health/route.ts`: Drive OAuthと承認済み保存先の読み戻し照合
- `app/api/admin/google-drive/sync/route.ts`: 管理者認証付きのDrive再格納
- `app/api/admin/readiness/route.ts`: 秘密値を返さない本番稼働条件の一括確認
- `app/api/staff/google-drive/sync/route.ts`: 権限を付与された採用担当者による面接単位のDrive再格納
- `app/api/staff/google-drive/retry/route.ts`: 自動停止中の1件だけを、担当者操作で最大1回安全再試行
- `app/api/interviews/recording/route.ts`: 認証付き録画アップロード
- `app/api/evaluate/route.ts`: 構造化評価
- `lib/interview-persistence.ts`: D1／R2保存、認証、監査ログ
- `lib/google-drive-sync.ts`: 候補者フォルダ、成果物、PDF、読戻し照合、再同期制御
- `lib/interview-invite.ts`: 招待URLのHMAC署名、期限、改ざん検知
- `db/schema.ts`: 面接セッション、成果物、監査イベントのスキーマ
- `lib/interview.ts`: 面接官プロンプトと共通定義
- `lib/interview-knowledge.ts`: Google Docs／Sheets／Notionから反映した正本質問・評価アンカー
- `lib/evaluation.ts`: 評価スキーマと根拠検証
- `public/tokyo-dogs-logo.jpg`: 支給ロゴの作業用コピー
- `public/interviewer-woman-medium-v2.png`: 東京DOGSのコーポレートカラーで作成した、実在人物ではないミディアムヘアのオンライン採用担当者茂木のポートレート
- `public/og-online-first-interview-v3.png`: オンライン一次面接ポータル用のSNS共有画像
- `tests/`: API・評価・画面テスト
- `scripts/mobile-test-gateway.mjs`: 許可された確認環境だけを外部確認可能にする経路制限

## 2026-08-26 同時3名の確認

- 同じ共通URLから3名が**それぞれ独立した面接**に参加する構成です。3名が同じ会議室へ入るグループ面接ではありません。アプリに2名で止める制限はなく、通常のDrive復旧対象も最大3件です。外部音声サービスの利用枠や各端末・回線の正常性まで保証する上限ではありません。
- 3名の同時音声接続、文字起こし・根拠付き評価の分離、候補者別Driveフォルダ、1件の失敗による他候補への影響防止を自動テストしています。
- 今回、実際の録画送信クライアントとビルド済みAPIを組み合わせ、3名が同時に分割送信する検査を追加しました。1名の保存応答消失と別の1名の確定書込失敗から自動再送し、各候補者の録画再取得バイト一致、重複なし、他候補者の資格情報での書込拒否を確認しました。ストレージ・外部サービスはテスト用代替で、実機試験ではありません。
- `npm run check`: 283 tests pass、Lint・型検査・build合格、production依存脆弱性0件。
- 直近2件の本番面接は、各6ファイルがDriveに存在し、動画サイズがシステム保存記録と一致しました。一方、全48同期記録のうちfailed 23・running 6が残存しています。全件正常・無人運用可能とは判定していません。
- 3台の実機（iPhone Safari／Android Chrome等）で15〜27分の同時面接を行い、双方向音声と全録画・全文文字起こし・保存完了を検証する受入試験は未完了です。
- 今回の変更は検査・文書のみです。人数のための本番設定変更・再デプロイは不要であり、実行していません。詳細は親フォルダの `70_同時3名_本番再確認_2026-08-26.md` を参照してください。

## 2026-08-28 Drive保存障害の停止・通知

- `GOOGLE_DRIVE_API_404`／`410` は永続障害として1回で自動停止し、候補者・担当者画面・cronから再開しません。
- 429／5xx／524など一時障害は、10分から指数的に間隔を広げ、通算4回で自動停止します。D1の `failure_count`、`next_retry_at`、`retry_blocked_at` が再試行状態の正本です。
- 保存失敗は `interview_operational_alerts` に候補者別で永続保存し、担当者画面に「保存未完了」「自動再試行停止」を表示します。画面を開いている場合は新規失敗を端末通知できます。
- 旧29件は、404反復22件を既存Drive受領情報を残したまま停止し、旧running 6件と524 1件だけを最大1回の安全復旧へ移します。削除、同名フォルダ新設、無制限再送は行いません。
- 自動停止後に認証・保存先を直した場合だけ、対象記録を開き「安全に1回だけ再試行」を押します。失敗時は直ちに再停止し、監査ログと保存失敗通知を更新します。
